# vike-content-collection

> Type-safe, schema-validated content collections for Vike + Vite. Define a Zod schema, drop in your markdown or MDX files, and get fully typed content with validated frontmatter -- at dev time and build time.

## Docs

- [Getting Started](docs/getting-started.md): Installation, project setup (Vite plugin, Vike config, tsconfig), creating your first collection, project structure
- [Defining Collections](docs/defining-collections.md): Schema formats (simple and extended), export styles, collection naming, content (md/mdx) vs data collections, content directory configuration, validation errors
- [Querying Data](docs/querying-data.md): getCollection(), getCollectionEntry() with all filter types, entry shape, usage patterns (blog listing, single post, combining collections, pagination)
- [Rendering Content](docs/rendering.md): renderEntry() for content-to-HTML, MDX rendering, custom renderers (ContentRenderer interface), extractHeadings(), custom remark/rehype plugins
- [TypeScript Setup](docs/typescript-setup.md): Generated declaration file, tsconfig.json configuration, virtual module type declaration, how CollectionMap augmentation works, troubleshooting
- [Advanced Features](docs/advanced-features.md): Computed fields, collection references, custom slugs, draft mode, sorting, pagination, grouping, breadcrumbs, next/previous navigation, TOC tree, collection tree, related entries, merge collections, unique values, entry URL, content series, i18n locales, git last modified, custom content renderers, virtual module, plugin options
- [Internationalization](docs/i18n.md): Multilingual content setup, slug suffix vs metadata strategy, folder structure, language switcher, localized breadcrumbs and rendering

## API Quick Reference

Functions exported from `vike-content-collection`:
- `vikeContentCollectionPlugin(options?)` -- Vite plugin factory (also the default export)
- `getCollection(name)` -- retrieve all entries of a collection, fully typed
- `getCollectionEntry(name, filter)` -- retrieve entries by slug (string), pattern (RegExp), predicate (function), or array of filters
- `renderEntry(entry, options?)` -- render content entry to HTML using default or custom renderer, returns `{ html, headings }`
- `extractHeadings(content)` -- extract headings from raw markdown without full render
- `createMarkdownRenderer(defaults?)` -- create a markdown renderer (default, used by renderEntry when no renderer specified)
- `createMdxRenderer(defaults?)` -- create an MDX renderer for .mdx files with JSX syntax support
- `sortCollection(entries, key, order?)` -- sort entries by a metadata key
- `paginate(entries, { pageSize, currentPage })` -- paginate an array of entries
- `groupBy(entries, key)` -- group entries by a metadata key (supports array values like tags)
- `getBreadcrumbs(collectionName, slug?, options?)` -- generate breadcrumb trail from collection path
- `getAdjacentEntries(name, currentSlug, options?)` -- find previous/next entries in a collection
- `getCollectionTree()` -- get all collections as a hierarchical tree
- `buildTocTree(headings)` -- convert flat headings into a nested TOC tree
- `getRelatedEntries(name, slug, options)` -- find entries related by shared metadata values
- `mergeCollections(names)` -- combine entries from multiple collections
- `uniqueValues(entries, key)` -- extract unique values for a metadata key
- `getEntryUrl(collectionName, slug, options?)` -- generate a URL path for an entry
- `getSeries(name, slug, seriesName, options?)` -- get ordered content series with navigation
- `getAvailableLocales(name, baseSlug, options?)` -- get available locales for an entry
- `getLocalizedEntry(name, baseSlug, locale, options?)` -- get a localized version of an entry
- `reference(collectionName)` -- create a cross-collection reference Zod schema

Config export from `vike-content-collection/config`:
- Default export extending Vike config to recognize `+Content.ts` files

## Key Concepts

- The plugin automatically provides no-op stubs for client-side bundles, keeping Node.js code out of the browser. Runtime functions like `getCollection()` return safe empty values on the client.
- `vike-content-collection` must be listed in `ssr.external` in `vite.config.ts` so Vite does not bundle it during SSR
- A `+Content.ts` file defines a collection by exporting a Zod schema (or `{ schema, computed?, slug?, type?, contentPath? }`)
- Collection names are derived from directory paths (e.g. `pages/blog/+Content.ts` → `"blog"`)
- Content collections (default): `.md` and `.mdx` files with YAML frontmatter validated against the schema
- Data collections (`type: 'data'`): `.json`, `.yaml`/`.yml`, `.toml` files validated against the schema
- Rendering is pluggable via the `ContentRenderer` interface; built-in renderers for markdown and MDX
- Type declarations are auto-generated at `.vike-content-collection/types.d.ts`
- Virtual module `virtual:content-collection` exposes serialized collection data to other plugins

## Plugin Options

- `contentDir` (string, default `"pages"`) -- directory to scan for `+Content.ts` files
- `contentRoot` (string, default same as contentDir) -- directory where content/data files live
- Per-collection `contentPath` (string, in +Content.ts) -- override the folder inside the content root for a specific collection
- `drafts.field` (string, default `"draft"`) -- metadata field for draft status
- `drafts.includeDrafts` (boolean, default true in dev / false in prod) -- force include/exclude drafts
- `lastModified` (boolean, default false) -- populate lastModified from git history

## Requirements

- Node.js >= 18
- Vite >= 7.0.0
- Vike >= 0.4.250
- Zod >= 3.0.0

## Optional

- [Full documentation](llms-full.txt)
